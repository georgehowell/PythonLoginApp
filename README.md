# PythonLoginApp
Login System with 'Register', validation, and password generator

## Run App:
`python index.py`


### Test Runner:
`cd tests`
#### when: "import unittest"
`python -m unittest`

#### when: "import pytest"
`python3 -m pytest`